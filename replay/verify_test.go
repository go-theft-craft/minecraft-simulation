package replay_test

import "testing"

// TestRecordingsAreReproducible is the one test the determinism matrix runs.
//
// It is a placeholder until the recordings exist. A green matrix on an empty
// check is still a real result — it says the six targets start and run Go — but
// it is not the gate, and it must not be mistaken for one.
func TestRecordingsAreReproducible(t *testing.T) {
	t.Skip("no recordings yet: this check is a placeholder while the matrix is proven to start")
}
